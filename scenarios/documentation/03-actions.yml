---
# 3. Actions to perform during an experiment
# Inject actions on a node at a certain time
#
# For example inject faults, change service or application behaviour
# Here are actions listed that are executed during an experiment
#
# IDEA: Put actions into a script. The run_experiment script also starts the
#   action script.
#

actions: {}
